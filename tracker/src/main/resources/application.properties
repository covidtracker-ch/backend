spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5450}/${DB_NAME:tracker}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:db}
app.secret=notsecret
